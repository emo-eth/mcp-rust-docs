version: "3.8"

services:
    mcp-rust-docs:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: mcp-rust-docs
        restart: unless-stopped
        stdin_open: true
        tty: true
        environment:
            - NODE_ENV=production
